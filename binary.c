#include <stdio.h>
int main (void)
{
int d, b, n;


printf("Input a decimal number to convert to binary.\n");
scanf("%d", &d);

while ()
r = d % 2 // Need to learn arrays
    



printf("%d in binary is (%d)", ?? , b);
return 0;
}
